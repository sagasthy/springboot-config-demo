app:
  name: Spring Config Demo
  description: Welcome to ${app.name}

my:
  greeting: Hello World
  list:
    values: One,Two,Three

db:
  connection: {connectionString: 'http://qat-db', userName: 'qat_foo', password: 'qat_pass'}
  host: 127.0.0.1
  port: 8081

#endpoints:
#  health:
#    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

